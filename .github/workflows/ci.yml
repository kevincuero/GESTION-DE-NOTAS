name: CI - Campus Project (Windows)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Verificar que el proyecto arranca (modo seguridad)
        run: |
          # Creamos un pequeño script de verificación y lo ejecutamos
          echo "import main" > check_start.py
          echo "print('Aplicación importada exitosamente.')" >> check_start.py
          python check_start.py
